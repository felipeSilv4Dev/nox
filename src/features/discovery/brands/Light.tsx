const Light = () => {
  return (
    <span className="absolute top-[40%] -left-[30%] -z-1 block h-[30%] w-[30%] rounded-full bg-linear-to-r from-blue-800 to-blue-500 blur-[10vw]" />
  );
};

export default Light;
